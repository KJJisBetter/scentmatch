# ScentMatch Environment Variables Template
# Copy this to .env.local and fill with your actual values

# === SUPABASE (Already in Vercel) ===
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ... (your anon key)
SUPABASE_SERVICE_ROLE_KEY=eyJ... (your service role key)

# === AI SERVICES (Missing - Need to Add) ===
OPENAI_API_KEY=sk-proj-... (get from https://platform.openai.com/api-keys)

# === OPTIONAL ===
VOYAGE_AI_API_KEY=pa-... (optional, for embeddings)
UPSTASH_REDIS_REST_URL=https://... (optional, for rate limiting)
UPSTASH_REDIS_REST_TOKEN=... (optional, for rate limiting)

# === ENVIRONMENT ===
NODE_ENV=development  # or production